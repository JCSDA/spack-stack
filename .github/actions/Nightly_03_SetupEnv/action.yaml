name: Nightly 03 Setup Env

runs:
  using: "composite"
  steps:
  - name: Nightly 03 Setup Env
    shell: bash
    run: |
      RUNID=`date +"%Y%m%d"`
      cd util/weekly_build
      ./03_SetupEnv.sh ${RUNID} ${BASEDIR} ${PLATFORM}
