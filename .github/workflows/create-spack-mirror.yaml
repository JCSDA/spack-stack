name: create-spack-mirror

on:
  workflow_dispatch:
    inputs:
      template:
        description: 'Base spack.yaml template. Default is an empty environment.'
        required: false
        default: 'empty'
      specs:
        description: 'Which application env to build'
        required: false
        default: 'jedi-ufs-env'

jobs:
  create-spack-mirror:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v2
        with:
          submodules: true

      - name: create-env
        run: |
          source ./setup.sh
          spack stack create env --template ${{ github.event.inputs.template }} --specs ${{ github.event.inputs.specs }} --name mirror
          spack env activate -d envs/mirror
          spack compiler find
          spack add re2c
          spack add clingo
          spack concretize
          spack mirror create -a
          # upload-artifact does not allow filenames that contain '?'
          rm -vf `find cache/source_cache -name '*\?*'`

      - name: upload-mirror
        uses: actions/upload-artifact@v2
        with:
          name: spack_mirror
          path: cache/source_cache
