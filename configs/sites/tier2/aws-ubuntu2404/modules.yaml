modules:
  default:
    enable::
    - lmod
    lmod:
      include:
      # Compiler modules
      - apple-clang
      - gcc
      - intel-oneapi-compilers
      - llvm
      # MPI modules
      - cray-mpich
      - intel-mpi
      - intel-oneapi-mpi
      - mpich
      - mpt
      - openmpi
