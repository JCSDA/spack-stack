modules:
  default:
    enable::
    - lmod
    lmod:
      whitelist:
      # List of packages for which we need modules that are blacklisted by default
      - intel-oneapi-mpi
      - openmpi
      - mpich
