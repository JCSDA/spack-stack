# Add-on environment to provide gsi-env with gsi-ncdiag@1.1.3
# See https://github.com/JCSDA/spack-stack/issues/1938
#
spack:
  concretizer:
    unify: when_possible

  view: false
  include: []

  specs:
  - gsi-env ^crtm@3.1.2 # match unified-dev template

  repos: [$env/envrepo]

  packages:
    all:
      buildable: false
    gsi-ncdiag::
      require: ['@1.1.3']
      buildable: true
    gsi-env:
      buildable: true
