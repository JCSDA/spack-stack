spack:
  concretizer:
    unify: when_possible

  include: []

  packages:
    all:
      compiler: [gcc]

   # Can't build jedi-ewok-env on macOS (12) with gcc (11)
  specs:
  - jedi-fv3-env
  - jedi-tools-env
  - jedi-ufs-env
  - jedi-um-env
  - nceplibs-env
  - soca-env
