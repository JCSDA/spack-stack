spack:
  concretization: separately
  view: false
  include:
  - config.yaml
  - packages.yaml
  - modules.yaml
  - site.yaml
  - ${SPACK_STACK_DIR}/configs/repos/repos.yaml

  specs:
  - jedi-fv3-bundle-env
