packages:
    all:
      prefer:
      - "%[when=%c]c=llvm %[when=%cxx]cxx=llvm %[when=%fortran]fortran=llvm"
    # Virtual provider settings
    blas:
      require: [openblas]
    fftw-api:
      require: [fftw]
    lapack:
      require: [openblas]
    # Individual package settings
    ectrans:
      require:
      - +fftw
